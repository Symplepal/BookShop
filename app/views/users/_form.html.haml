= simple_form_for(@user) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, label: 'Név'
    = f.input :address, label: 'Cím'
    = f.input :email

  .form-actions
    = f.button :submit, 'Bevitel'
