.nested-fields
  = f.input :purchase_id, as: :hidden, input_html: {value: @purchase.id}
  = f.input :book_id, collection: Book.all, label_method: :title, value_method: :id
  = f.input :quantity
  = link_to_remove_association "Könyv kivétele a kosárból", f