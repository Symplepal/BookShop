%h1 Listing books
%table
  %thead
    %tr
      %th Title
      %th Author
      %th Publisher
      %th Published date
      %th Price
      %th Page count
      %th Genre
      %th{:colspan => "3"}
  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.author_id
        %td= book.publisher_id
        %td= book.published_date
        %td= book.price
        %td= book.page_count
        %td= book.genre_id
        %td= link_to 'Show', book
        %td= link_to 'Edit', edit_book_path(book)
        %td= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Book', new_book_path
