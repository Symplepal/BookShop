%h1 Könyvek
%table.table.table-striped
  %thead
    %tr
      %th Cím
      %th Szerző
      %th Kiadó
      %th Kiadás dátuma
      %th Ár
      %th Oldalszám
      %th Műfajok
      %th{:colspan => "3"}
  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.author.name
        %td= book.publisher.name
        %td= book.published_date.strftime('%d-%m-%Y')
        %td
          = book.price
          Ft
        %td= book.page_count
        %td
          - book.genres.each do |genre|
            = genre.name
        %td= link_to 'Megtekintés', book
        %td= link_to 'Szerkesztés', edit_book_path(book)

%br/
= link_to 'Új könyv felvétele', new_book_path
